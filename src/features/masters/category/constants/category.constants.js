export const CATEGORY_TABLE_COLUMNS = [
  { key: 'name', label: 'Name', sortable: true },
  { key: 'sortOrder', label: 'Sort Order', sortable: true },
  { key: 'itemCount', label: 'Items', sortable: false },
];

import { PLACEHOLDER_IMAGE } from '../../../../lib/imagePlaceholder';

export const DEFAULT_CATEGORY_IMAGE = PLACEHOLDER_IMAGE;
